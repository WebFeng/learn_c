1.shell程序默认打开三个文件,对应的文件描述符为0,1,2
/*低级I/O read&write*/
int n_read = read(int fd, char *buf, int n);
int n_write = write(int fd, char *buf, int n);
//每次调用返回实际传输字节数
//读文件时返回值可能小于请求字节数
//返回为0代表到达文件末尾
//返回为-1代表发生某种错误
//写文件时返回写入字节数,如果返回值与请求写入字节数不等,则代表发生错误
{无缓冲:每次读取或写入1字节}
//用更大的值调用IO函数更高效,因为减少了系统调用次数


